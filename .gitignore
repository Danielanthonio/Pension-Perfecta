# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env
.env*.local

# local secrets (DB connection string for manual backups — never commit)
.secrets/

# local scratch / debugging scripts (never deploy; may contain old/inert creds)
scratch/
architecture-output.md

# fotos de referencia para generar portadas (personales — no van al repo).
# Las portadas YA generadas sí se versionan, en public/classroom/.
pic raul/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
next-build.zip
extract-architecture.sh
